
	GitLab URL          : http://${ip4_addr}
	GitLab root login   : root
	GitLab root passowrd: "yourpassword"

	Attention! By default, the image does not use external volumes.
	Refer to the manual on how to use external volumes before jail start:

	URL: https://gitlab.fechner.net/mfechner/gitlab-cbsd/#external-volume

	Hint:
	Enable cluster mode if you expect to have a high load instance
	Ex. change amount of workers to 3 for 2GB RAM server
	Set the number of workers to at least the number of cores:

	vi /usr/local/www/gitlab/config/puma.rb
